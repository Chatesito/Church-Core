// src/pages-flat/home/ui/HomePage.tsx
// Using "pages-flat" to avoid naming conflicts with Next.js "app" folder routing

import { HomeSection } from '@/widgets';

export default function HomePage() {
  return <HomeSection />;
}
